<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp">


    <de.mide.android.customview.BalkenDiagrammView
        android:id="@+id/balkendiagramm"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/neuDatenButton"
        android:background="@drawable/schwarzer_rand"
        app:abstandObenUnten="1" />
    <!-- das letzte Attribut ist ein "Custom Attribute" (siehe auch res/values/attrs.xml)-->

    <Button
        android:id="@+id/neuDatenButton"
        android:onClick="onNeueDatenButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:text="@string/button_neue_daten"
        android:textSize="20sp"
        android:layout_alignParentBottom="true" />

</RelativeLayout>